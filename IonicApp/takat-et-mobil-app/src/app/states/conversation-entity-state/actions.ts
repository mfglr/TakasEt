import { createAction } from "@ngrx/store";

export const nextPageAction = createAction("[Conversation Entity Store]")
export const nextPageSuccessAction = createAction("[Conversation Entity Store]",)
